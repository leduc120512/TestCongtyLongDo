{
  "name": "@longdo/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API phân hệ Công việc — Fastify 5 + MongoDB",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts",
    "seed": "node src/seed.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/jwt": "^10.2.0",
    "@longdo/contracts": "workspace:*",
    "fastify": "^5.12.0",
    "mongodb": "^7.6.0",
    "zod": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=22.18"
  }
}
