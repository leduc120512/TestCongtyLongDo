# Sao chép thành .env nếu muốn đổi. Không commit .env có giá trị thật.
PORT=3000
HOST=127.0.0.1
MONGO_URL=mongodb://localhost:27017/longdo_congviec?directConnection=true
JWT_SECRET=doi-bi-mat-nay-khi-chay-that
# Bỏ # ở dòng dưới khi chạy thật: bắt buộc JWT_SECRET >= 32 ký tự, tắt đăng nhập giả lập, không cho chạy seed.
# NODE_ENV=production
